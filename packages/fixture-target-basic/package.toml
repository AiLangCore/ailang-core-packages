schema = "ailang.package-source.v1"
name = "fixture-target-basic"
version = "0.0.1-test.1"
types = ["target"]

[targets.fixture-basic]
id = "fixture-basic"
description = "Minimal fixture target for testing target discovery"
runtime = "aivm"
profile = "generic"
artifactTypes = ["dir"]
