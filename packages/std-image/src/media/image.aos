Program {
  Let(name=decodeToRgbaBase64) {
    Fn(params=data,mimeType) {
      Block {
        Return {
          Call(target=sys.image.decodeToRgbaBase64) {
            Var(name=data)
            Var(name=mimeType)
          }
        }
      }
    }
  }

  Export(name=decodeToRgbaBase64)
}
